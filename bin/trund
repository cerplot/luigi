#!/usr/bin/env python

import sys
import warnings
import trun.cmdline


def main(argv):
    warnings.warn("'bin/trund' has moved to console script 'trund'", DeprecationWarning)
    trun.cmdline.trund(argv)


if __name__ == '__main__':
    main(sys.argv[1:])
